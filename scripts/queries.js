

// Queries from highest to smallest screen sizes, going from top to bottom of this script



// const reportWindowSize = (med) => {

//     med.currentTarget.innerWidth===1075 ? log(true):log(false);
//     switch (med.currentTarget.innerWidth) {
//         case 1075:
//             log(1075);
//             break;
//     };
// };

// window.addEventListener('resize', reportWindowSize);